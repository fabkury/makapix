{
	# Global options
	log {
		output file /var/log/caddy/access.log
		format json
		level INFO
	}
}

# Import docker-generated config (caddy-docker-proxy will inject routes here)
import /config/caddy/Caddyfile
