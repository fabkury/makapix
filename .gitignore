servoom/
credentials.py

# Environment files (contain secrets - must be gitignored)
.env
.env.*
docker-compose.override.yml

# Keep template files (these are safe to commit)
!env.*.template
!docker-compose.override.*.yml

# Generated Caddyfile
proxy/Caddyfile

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
*.log
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.next/
out/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker
*.pid

# MQTT certs (except templates)
mqtt/certs/*.crt
mqtt/certs/*.key
mqtt/certs/*.pem
mqtt/certs/*.srl
mqtt/certs/*.csr

# MQTT password file (contains hashed passwords - generate with mosquitto_passwd)
mqtt/config/passwords
!mqtt/config/passwords.example


# Temp files
api/temp/*
!api/temp/.gitkeep

# Stack deployment stats (gitignored, but example is tracked)
deploy/stack/cta-stats.csv
!deploy/stack/cta-stats.csv.example

# GeoIP database files (download from MaxMind, see api/app/geoip/README.md)
*.mmdb
api/app/geoip/*.mmdb
