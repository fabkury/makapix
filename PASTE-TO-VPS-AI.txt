I need you to set up the Makapix remote development environment on this VPS for dev.makapix.club. Here are the steps:

1. Navigate to the repo and pull latest changes:
   cd ~/makapix/repo
   git pull origin main

2. Run the initial setup to create environment files:
   ./scripts/setup-dev.sh

3. Edit .env.remote to add my GitHub credentials for dev.makapix.club:
   nano .env.remote
   
   I need to set:
   - GITHUB_OAUTH_CLIENT_ID=<my_remote_oauth_client_id>
   - GITHUB_OAUTH_CLIENT_SECRET=<my_remote_oauth_secret>
   - GITHUB_APP_ID=<my_remote_app_id>
   - GITHUB_APP_PRIVATE_KEY=<my_remote_private_key>
   - JWT_SECRET_KEY=<strong_random_secret>
   - POSTGRES_PASSWORD=<strong_db_password>

4. Switch to remote environment:
   make remote

5. Stop any existing containers:
   make down

6. Start all services:
   make up

7. Verify everything is running:
   make status
   curl https://dev.makapix.club/api/health

8. If there are issues, show me the logs:
   make logs

Important prerequisites:
- DNS for dev.makapix.club must point to this VPS IP
- Ports 80 and 443 must be open
- Docker must be installed and running

Please execute these steps and let me know the results at each stage.

